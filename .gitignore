.venv/
__pycache__/
*.py[cod]

outputs/
calibrations/
*.png
*.jpg
*.jpeg
*.mp4

# C++ build / TensorRT engines
cpp/build/
cpp/.cache/
windows/**/build*/
windows/**/out/
models/
*.engine
*.plan
*.trt
compile_commands.json

# Editor
.vscode/
.idea/
.antigravitycli/

# Per-machine `cpp/build/main --config PATH` files. Samples are tracked as
# `configs/*.yaml.example`; copy to `configs/*.yaml` and edit locally.
configs/*.yaml
# Named config profiles written by the WebUI Setup mode — per-machine, not
# tracked (same rationale as configs/*.yaml above).
configs/named/
