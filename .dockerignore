.git
.github
.gitignore
.vscode
.claude

cpp/build
cpp/build-*

outputs

python/.venv
python/__pycache__
**/__pycache__
**/*.pyc

# Web assets are needed at runtime; keep them in the image.
# Exclude only generated artifacts.
web/**/node_modules
# web-ui source is built in the webbuild stage; never ship host node_modules/dist.
web-ui/node_modules
web-ui/dist

# Editor / OS noise
.DS_Store
*.swp
